services:
  app:
    build:
      context: .
    # Uses variables passed from Jenkins, falls back to latest if empty
    image: ${IMAGE_NAME}:${IMAGE_TAG:-latest}
    ports:
      - "8091:8082"
    restart: "always"
